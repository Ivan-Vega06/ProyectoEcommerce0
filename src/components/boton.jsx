export const Boton = () => {
    const estilos = {
        backgroundColor: color,
        color: "white",
    };
};

const saludar = () => {
    alert("Boton Clikeado")
};

return (
    <button style={estilos} onClick={saludar}>
        {texto}
    </button>
);
